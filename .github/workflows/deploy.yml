name: Deploy Frontend

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm ci

      - name: Create env file
        run: |
          echo "REACT_APP_BACKEND_URL=${{ secrets.REACT_APP_BACKEND_URL }}" > .env
          echo "REACT_APP_ENCRYPTION_SECRET=${{ secrets.REACT_APP_ENCRYPTION_SECRET }}" >> .env

      - name: Build frontend
        run: CI=false npm run build

      - name: Deploy build to Nginx
        run: |
          rsync -avz --delete ./build/ /var/www/frontend/
          sudo systemctl reload nginx
